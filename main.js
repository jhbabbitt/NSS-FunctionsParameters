const quarterValue = (bigNumber) => {
    const quarter = bigNumber / 4
    return quarter
}

// Invoke the function and store the return value
const quarteredNumber = quarterValue(444)

// Log the return value
console.log(quarteredNumber)